name: Microcopy Format Fix

on:
  push:
    branches:
      - microcopy-format-fix

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: gosso-admin-frontend
    steps:
      - uses: actions/checkout@v4
        with:
          ref: microcopy-format-fix
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: gosso-admin-frontend/package-lock.json
      - run: npm ci
      - run: npx prettier --write src/pages/account-settings/PasswordPanel.tsx src/pages/system-management/SiteSettingsTab.tsx src/pages/system-management/UsersTab.tsx
      - name: Commit exact Prettier output
        working-directory: .
        run: |
          if git diff --quiet; then
            echo "No formatting changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add gosso-admin-frontend/src/pages/account-settings/PasswordPanel.tsx gosso-admin-frontend/src/pages/system-management/SiteSettingsTab.tsx gosso-admin-frontend/src/pages/system-management/UsersTab.tsx
          git commit -m "style(gosso): apply exact Prettier formatting"
          git push origin HEAD:microcopy-format-fix
