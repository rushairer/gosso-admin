name: UI Browser Acceptance

on:
  pull_request:
    branches:
      - main
    paths:
      - "gosso-admin-frontend/**"
      - ".github/workflows/ui-browser-acceptance.yml"
  push:
    branches:
      - main
    paths:
      - "gosso-admin-frontend/**"
      - ".github/workflows/ui-browser-acceptance.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ui-browser-acceptance-${{ github.ref }}
  cancel-in-progress: true

jobs:
  browser-acceptance:
    name: Gosso Admin rendered acceptance
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: gosso-admin-frontend

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: gosso-admin-frontend/package-lock.json

      - name: Install product dependencies
        run: npm ci

      - name: Install isolated Playwright runner
        run: cd e2e && npm install --package-lock=false

      - name: Install Chromium
        run: cd e2e && npx playwright install --with-deps chromium

      - name: Run rendered acceptance
        run: cd e2e && npx playwright test --config=playwright.config.mjs

      - name: Retain browser evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: gosso-admin-browser-acceptance
          path: |
            gosso-admin-frontend/playwright-report
            gosso-admin-frontend/test-results
          if-no-files-found: warn
          retention-days: 14
